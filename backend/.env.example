# GitHub personal access token — raises API rate limits, required for private repos.
# Create one at https://github.com/settings/tokens (read-only repo scope is enough)
GITHUB_TOKEN=

# Used to summarize commits/diffs into changelog prose.
# Free tier available — get a key at https://aistudio.google.com/apikey
GEMINI_API_KEY=

PORT=3001
